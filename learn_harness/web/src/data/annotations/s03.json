{
  "version": "s03",
  "decisions": [
    {
      "id": "permission-before-execution",
      "title": "Gate at the Harness Level, Not the Tool Level",
      "description": "Permissions are checked in the harness, before the tool executes. This means every tool gets permission checking for free — no tool needs to implement its own security logic.",
      "alternatives": "Each tool could implement its own permission checks, but that leads to inconsistent behavior and security gaps. A harness-level gate is the single point of control.",
      "zh": { "title": "在 Harness 层而非工具层加权限门", "description": "权限在 harness 层检查，在工具执行之前。这意味着每个工具自动获得权限检查——不需要各自实现安全逻辑。", "alternatives": "每个工具可以实现自己的权限检查，但那会导致行为不一致和安全漏洞。harness 层的权限门是单一控制点。" }
    },
    {
      "id": "user-confirmation",
      "title": "User Must Opt In for Dangerous Actions",
      "description": "Commands that modify the filesystem, access the network, or execute arbitrary code require explicit user confirmation. Read-only operations pass through automatically.",
      "alternatives": "A full auto-approve mode exists for trusted scenarios, but the default is ask-per-action to prevent surprise side effects.",
      "zh": { "title": "危险操作需要用户确认", "description": "修改文件系统、访问网络或执行任意代码的命令需要用户明确确认。只读操作自动放行。", "alternatives": "对于可信场景存在完全自动批准模式，但默认是每次询问，以防止意外的副作用。" }
    }
  ]
}
